"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {LockKeyhole,ShieldCheck,AlertCircle} from "lucide-react";import {useAuth} from "@/components/AuthProvider";
const destinations:Record<string,string>={ADMIN:"/admin",DIRECTOR:"/admin",COORDINATOR:"/admin-admissions",BURSAR:"/admin/fees",HOD:"/teacher/dashboard",TEACHER:"/teacher/dashboard",PARENT:"/parent/dashboard",STUDENT:"/student/dashboard"};
export default function LoginPage(){const router=useRouter();const{refresh}=useAuth();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const body=await r.json();if(!r.ok)throw new Error(body.error);await refresh();router.replace(destinations[body.user.role]||"/")}catch(x){setError(x instanceof Error?x.message:"Unable to sign in.")}finally{setLoading(false)}};return <main className="min-h-screen bg-brand-navy px-6 py-16"><div className="mx-auto grid max-w-5xl overflow-hidden rounded-[2rem] border border-white/10 bg-white shadow-2xl md:grid-cols-2"><section className="bg-brand-navy p-9 text-white md:p-12"><Link href="/" className="text-xs font-bold uppercase tracking-widest text-brand-green">← Ykay College</Link><ShieldCheck className="mt-14 text-brand-green" size={42}/><h1 className="mt-5 font-display text-4xl tracking-widest">EDUPORTAL<br/><span className="text-brand-green">SECURE ACCESS</span></h1><p className="mt-5 max-w-sm text-sm leading-7 text-white/65">Sign in with the credentials issued to you by Ykay College. For account help, contact the school administration.</p></section><section className="p-9 md:p-12"><h2 className="font-display text-3xl tracking-widest text-brand-navy">SIGN IN</h2><p className="mt-2 text-sm text-slate-500">Use your registered school email address.</p>{error&&<p role="alert" className="mt-5 flex gap-2 rounded-xl bg-red-50 p-3 text-sm text-red-700"><AlertCircle size={17}/>{error}</p>}<form onSubmit={submit} className="mt-7 space-y-5"><label className="block text-xs font-bold uppercase tracking-widest text-slate-700">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 text-sm"/></label><label className="block text-xs font-bold uppercase tracking-widest text-slate-700">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 text-sm"/></label><button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-full bg-brand-green px-5 py-4 text-sm font-bold uppercase tracking-widest text-white disabled:opacity-50"><LockKeyhole size={16}/>{loading?"Signing in":"Sign in"}</button></form><Link href="/reset-password" className="mt-6 block text-center text-sm font-semibold text-brand-green hover:underline">Forgot password?</Link></section></div></main>}
