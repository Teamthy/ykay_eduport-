"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{CheckCircle2,Clock3,FileText,XCircle}from"lucide-react";import Header from"@/components/Header";import Footer from"@/components/Footer";import{useAuth}from"@/components/AuthProvider";
type App={applicationId:string;firstName:string;lastName:string;classApplying:string;parentEmail:string;parentPhone:string;previousSchool:string;status:string;statusNote:string|null;submittedAt:string|null;documents:{id:string;type:string;fileName:string;sizeBytes:number}[];payment:{status:string;reference:string}|null};
const statuses=["PENDING_REVIEW","DOCUMENTS_REQUESTED","APPROVED","DECLINED","WAITLISTED"];
export default function AdminAdmissionsPage(){const{user,loading}=useAuth();const router=useRouter();const[apps,setApps]=useState<App[]>([]);const[selected,setSelected]=useState<App|null>(null);const[note,setNote]=useState("");const[status,setStatus]=useState("PENDING_REVIEW");const[msg,setMsg]=useState("");useEffect(()=>{if(!loading&&!user)router.replace("/login");if(user)fetch("/api/admin/admissions").then(r=>r.json()).then(j=>setApps(j.applications||[]))},[user,loading,router]);const update=async()=>{if(!selected)return;const r=await fetch("/api/admin/admissions",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({applicationId:selected.applicationId,status,note})});const j=await r.json();if(!r.ok){setMsg(j.error||"Unable to update.");return}setApps(a=>a.map(x=>x.applicationId===selected.applicationId?{...x,...j.application}:x));setSelected(null);setMsg("Application decision saved and parent email queued.")};if(loading)return null;return <><Header/><main className="min-h-screen bg-[var(--bg-primary)]"><section className="bg-brand-navy px-6 pb-12 pt-28 text-white"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold tracking-widest text-brand-green">PROTECTED ADMIN AREA</p><h1 className="mt-3 font-display text-5xl tracking-widest">ADMISSIONS QUEUE</h1><p className="mt-3 text-white/65">Review paid, submitted applications and record an accountable decision.</p></div></section><section className="mx-auto max-w-7xl px-6 py-10">{msg&&<p className="mb-5 rounded-xl bg-green-50 p-4 text-sm text-green-800">{msg}</p>}<div className="overflow-hidden rounded-3xl border border-[var(--border-subtle)] bg-[var(--surface-card)]"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-[var(--surface-disabled)] text-xs uppercase tracking-widest text-[var(--text-muted)]"><tr><th className="p-4">Applicant</th><th className="p-4">Class</th><th className="p-4">Documents</th><th className="p-4">Status</th><th className="p-4"></th></tr></thead><tbody>{apps.map(app=><tr key={app.applicationId} className="border-t border-[var(--border-subtle)]"><td className="p-4"><b>{app.firstName} {app.lastName}</b><br/><span className="font-mono text-xs text-[var(--text-muted)]">{app.applicationId}</span></td><td className="p-4">{app.classApplying}</td><td className="p-4">{app.documents.length}</td><td className="p-4">{app.status.replaceAll("_"," ")}</td><td className="p-4"><button onClick={()=>{setSelected(app);setStatus(app.status);setNote(app.statusNote||"")}} className="font-bold text-brand-green">Review</button></td></tr>)}</tbody></table>{!apps.length&&<p className="p-10 text-center text-sm text-[var(--text-muted)]">No submitted applications yet.</p>}</div></div></section>{selected&&<div className="fixed inset-0 z-[200] grid place-items-center bg-black/60 p-5"><div className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-white p-7"><div className="flex justify-between gap-4"><div><p className="text-xs font-bold tracking-widest text-brand-green">APPLICATION REVIEW</p><h2 className="mt-2 font-display text-3xl text-brand-navy">{selected.firstName} {selected.lastName}</h2><p className="mt-1 text-sm text-slate-500">{selected.applicationId} · {selected.classApplying}</p></div><button onClick={()=>setSelected(null)}><XCircle/></button></div><div className="mt-6 grid gap-4 rounded-2xl bg-slate-50 p-5 text-sm md:grid-cols-2"><p><b>Parent:</b> {selected.parentEmail}<br/>{selected.parentPhone}</p><p><b>Previous school:</b><br/>{selected.previousSchool}</p><p><b>Payment:</b> {selected.payment?.status||"Pending"}</p><p><b>Documents:</b> {selected.documents.map(d=>d.fileName).join(", ")||"None"}</p></div><label className="mt-6 block text-xs font-bold uppercase tracking-widest">Decision<select value={status} onChange={e=>setStatus(e.target.value)} className="mt-2 w-full rounded-xl border p-3">{statuses.map(s=><option key={s}>{s}</option>)}</select></label><label className="mt-5 block text-xs font-bold uppercase tracking-widest">Message to parent<textarea value={note} onChange={e=>setNote(e.target.value)} className="mt-2 w-full rounded-xl border p-3" rows={4}/></label><button onClick={update} className="mt-6 w-full rounded-full bg-brand-green py-3 font-bold text-white">Save decision and notify parent</button></div></div>}</main><Footer/></>}
