diff --git a/app/api/admin/students/route.ts b/app/api/admin/students/route.ts
index 4ee06e9..e353c93 100644
--- a/app/api/admin/students/route.ts
+++ b/app/api/admin/students/route.ts
@@ -13,7 +13,12 @@ const schema = z.object({
   classId: z.string().min(1),
   guardianName: z.string().trim().min(2).max(160),
   guardianPhone: z.string().trim().min(7).max(30),
-  guardianEmail: z.string().trim().toLowerCase().email().optional(),
+  // Empty string (an untouched optional field) must be treated as "not provided".
+  // zod's .optional() only handles undefined, so "" would fail .email() and 400.
+  guardianEmail: z.preprocess(
+    (v) => (typeof v === "string" && v.trim() === "" ? undefined : v),
+    z.string().trim().toLowerCase().email().optional(),
+  ),
 });
 export async function GET() {
   const user = await requireRole(PEOPLE_ADMIN_ROLES);
@@ -130,6 +135,25 @@ export async function POST(request: NextRequest) {
           guardianEmail: input.guardianEmail || null,
         },
       });
+      // Create a login account so this student can sign in with their full name
+      // + the shared student password (see the student branch in /api/auth/login).
+      const studentEmail = `${number
+        .replace(/[^a-zA-Z0-9]/g, "")
+        .toLowerCase()}@students.ykaycollege.com`;
+      const studentUser = await tx.user.create({
+        data: {
+          schoolId: user.schoolId,
+          email: studentEmail,
+          name: displayName,
+          role: "STUDENT",
+          passwordHash: await passwordHash(oneTimeSecret()),
+          mustChangePassword: false,
+        },
+      });
+      await tx.studentProfile.update({
+        where: { id: student.id },
+        data: { userId: studentUser.id },
+      });
       if (parentId)
         await tx.parentStudentLink.create({
           data: {
diff --git a/app/api/auth/login/route.ts b/app/api/auth/login/route.ts
index c2d82e0..187ebfb 100644
--- a/app/api/auth/login/route.ts
+++ b/app/api/auth/login/route.ts
@@ -8,8 +8,9 @@ import { resolveTenantFromHost } from "@/lib/tenant";
 import { recordSecurityEvent, getUserAgent } from "@/lib/forensics";
 import { enforceRateLimit } from "@/lib/rate-limit";
 
+// `email` holds an email address OR a student's full name (student quick-login).
 const schema = z.object({
-  email: z.string().trim().toLowerCase().email(),
+  email: z.string().trim().min(1),
   password: z.string().min(1),
 });
 
@@ -58,6 +59,66 @@ export async function POST(request: NextRequest) {
     const { tenant } = await resolveTenantFromHost(request.headers.get("host"));
     if (!tenant)
       return NextResponse.json({ error: "Sign-in is temporarily unavailable." }, { status: 503 });
+
+    // ── Student quick-login: full name + a single shared password ──────────
+    // Identifier is the student's full name (no "@"), password is the shared
+    // student password. Resolves the student profile → its linked User.
+    const sharedPw = process.env.STUDENT_SHARED_PASSWORD || "YKAYSTUDENT2026";
+    if (password === sharedPw && !email.includes("@")) {
+      const student = await prisma.studentProfile.findFirst({
+        where: {
+          schoolId: tenant.id,
+          isActive: true,
+          displayName: { equals: email, mode: "insensitive" },
+        },
+        include: { user: true },
+      });
+      if (!student?.user || student.user.isSuspended || !student.user.isActive) {
+        await bcrypt.compare(password, DUMMY_HASH); // timing parity
+        await recordSecurityEvent({
+          eventType: "LOGIN_FAILED_ACCOUNT_NOT_FOUND",
+          ipAddress: ip,
+          userAgent: ua,
+          reason: "No active student account for this name.",
+        });
+        return NextResponse.json({ error: "Invalid name or password." }, { status: 401 });
+      }
+      const token = await signSession({
+        id: student.user.id,
+        schoolId: student.user.schoolId,
+        role: student.user.role,
+        name: student.user.name,
+        email: student.user.email,
+        mustChangePassword: student.user.mustChangePassword,
+      });
+      await prisma.user.update({
+        where: { id: student.user.id },
+        data: { lastLoginAt: new Date() },
+      });
+      await prisma.auditLog.create({
+        data: {
+          schoolId: student.user.schoolId,
+          actorUserId: student.user.id,
+          action: "USER_SIGNED_IN",
+          entityType: "User",
+          entityId: student.user.id,
+          ipAddress: ip,
+          metadata: { method: "STUDENT_NAME" } as never,
+        },
+      });
+      const response = NextResponse.json({
+        user: {
+          name: student.user.name,
+          email: student.user.email,
+          role: student.user.role,
+          mustChangePassword: student.user.mustChangePassword,
+        },
+      });
+      const cookie = sessionCookie(token);
+      response.cookies.set(cookie.name, cookie.value, cookie.options);
+      return response;
+    }
+
     const user = await prisma.user.findFirst({ where: { email, schoolId: tenant.id } });
 
     // ── Timing-oracle mitigation ──────────────────────────────
diff --git a/app/login/page.tsx b/app/login/page.tsx
index 7223ddd..2461fe7 100644
--- a/app/login/page.tsx
+++ b/app/login/page.tsx
@@ -28,7 +28,7 @@ const portalContext: Record<string, { heading: string; sub: string; icon: typeof
     },
     student: {
       heading: "STUDENT SIGN IN",
-      sub: "Sign in with the student credentials issued by Ykay College.",
+      sub: "Enter your FULL NAME (in capitals) and the shared student password.",
       icon: User,
     },
     parent: {
@@ -120,14 +120,15 @@ function LoginForm() {
           )}
           <form onSubmit={submit} className="mt-7 space-y-5">
             <label className="block text-xs font-bold uppercase tracking-widest text-slate-700">
-              Email
+              Email or full name
               <input
                 required
-                type="email"
-                autoComplete="email"
+                type="text"
+                autoComplete="username"
                 value={email}
                 onChange={(event) => setEmail(event.target.value)}
                 className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 text-sm"
+                placeholder="you@ykaycollege.com · or your full name (students)"
               />
             </label>
             <label className="block text-xs font-bold uppercase tracking-widest text-slate-700">
